# CLAUDE.md

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This project's agent
instructions live in `AGENTS.md`; the import below pulls them into context so
both tools share one source of truth. Put any Claude-specific notes below the
import.

@AGENTS.md
